#include "main.h"

/**
 * print_alphabet - Check description
 * description: prints all the alphabets in lowervase
 * Return: Always (0) Success
 */

void print_alphabet(void)
{
	int n;

	for (n = 'a'; n <= 'z'; n++)
		_putchar(n);
	_putchar('\n');
}
